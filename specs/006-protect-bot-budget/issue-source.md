# Źródło wymagań — issue #869

Pobrano 2026-09-25; updatedAt: 2026-09-25T09:26:10Z; źródło: https://github.com/krzysztofcal/arcadePlatform/issues/869.
Kod porównany z aktualnym origin/main `93d0f191c3f87006d56f7afb2fb1c4052a7ecb84`; zmiany tej korekty dotyczą wyłącznie dokumentacji.
HEAD przed aktualizacją: `4d5c9cbf62328b4135bf8b5a774e32e4795dc068`. D.2 i D.3 zatwierdzone; poniżej pełna bieżąca treść issue, z jej własnym historycznym statusem prac.

# Poker — protected dual-rate bot budget, matchmaking and bounded bankroll refill

## Status and scope

**Product policy approved through 2026-09-25; implementation NOT started.** Codex (Astra) prepared a docs-only Spec Kit in [draft PR #1017](https://github.com/krzysztofcal/arcadePlatform/pull/1017). The latest independently reviewed plan head before this update is `477e88228fa1028bdaaae7365528e4d1191153ad`: D1–D3, multi-table drain, rolling slow, and separate FUNDING/EXPOSURE are documented. **Newly approved 2026-09-25: fail-fast, user-specific matchmaking and automatic find-or-create/reselection (section D1 below); these new requirements are NOT YET incorporated into the Spec Kit.** Astra must update the SAME draft PR and rerun Spec Kit analyze, then request independent review before any separate implementation task for Luna. This issue edit approves policy and planning only, NOT implementation, Stage migrations/seeding, WS deploy, Production mint/refill, or Production activation. This issue replaces the earlier broad anti-farming proposal with a concrete protection model. Goal: a single player who consumes their bot allowance must not be able to drain funding meant for everybody else or make an ordinary player's table wait for bot funding. An exhausted allowance is a funding state, not proof of cheating. Human-vs-human play, legal winnings and lawful cash-out remain unaffected.

Current V1: 100 CH bot funding uses legacy SYSTEM (default TREASURY), 500 CH uses one-time funded POKER_BOT_BANKROLL, higher tiers human-only. Do not expand bot tiers before #869 controls are verified (#870). This issue approves **planning/policy only**, not a Production mint, configuration change, migration or deployment.

## A. Agreed policy: two independently bounded resources, not two charges to a player

1. **Per-user access allowance**: durable server-authoritative entitlement to NEW bot-funded exposure, with fast and slow rates. It is NOT a USER chip account, a cap on the player's CH balance or a second currency; renewing it does not transfer or mint CH. A single user cannot reset it by table/tier switch, reconnect, reload, multiple tabs or duplicate requests.
2. **Real SYSTEM bankroll + issuance ceiling**: separate attributable bot funding per tier, independent of USER balances and of general TREASURY. Real money for seed/replacement/top-up comes from these SYSTEM pools. Their bounded refill must be an actual balanced, auditable ledger transaction with an absolute weekly cap. A user's renewed allowance cannot create bot liquidity if the real pool or its global issuance budget is empty.

**No unlimited refill, no unbounded mint, no silent TREASURY or cross-tier fallback.** One player's cap cannot guarantee infinite availability against an unbounded number of separate accounts; the global ceiling limits their combined economic impact. Do not describe the per-user budget as a second deduction from their CH.

## B. Initial dual-rate numbers (approved pilot policy; configurable, verify on Stage)

| Buy-in tier | Fast allowance per player per 7-day period | Slow allowance after fast exhaustion |
| --- | ---: | --- |
| 100 CH | 10,000 CH-equivalent = 100 full bot buy-ins | 100 CH-equivalent = 1 full bot buy-in / 12 h |
| 500 CH | 50,000 CH-equivalent = 100 full bot buy-ins | 500 CH-equivalent = 1 full bot buy-in / 12 h |
| 1,000 CH | 100,000 CH-equivalent = 100 full bot buy-ins | 1,000 CH-equivalent = 1 full bot buy-in / 12 h |
| 5,000 CH | 500,000 CH-equivalent = 100 full bot buy-ins | 5,000 CH-equivalent = 1 full bot buy-in / 12 h |
| 10,000 CH | 1,000,000 CH-equivalent = 100 full bot buy-ins | 10,000 CH-equivalent = 1 full bot buy-in / 12 h |

- Tiers above 500 CH are **configuration/design targets for #870 only**; do not activate their bots as part of #869. No automatic scaling above 10,000 CH: explicitly approve any higher-tier rates and global issuance pool before #870 enables it.
- Give each player ONE shared fast allowance of **100 full-bot-buy-in exposure units** per 7-day period across ALL enabled tiers; a bot buy-in of tier T uses one unit (partial funding uses funding delta / T), while actual CH exposure is separately recorded. Do not grant 100 units separately per tier. During the initially planned <=10,000 CH range, fast exposure is also bounded by **1,000,000 CH per player per period**; tier moves cannot reset the counters. Reconcile fractional units deterministically without rounding away tiny replacement funding.
- Start one fixed, persistent, per-account 7-day period on first eligible use; repeat on that persisted cadence. Fast allowance resets to 100 units at each period boundary, with no rollover/catch-up of unused units. Do not use a continuously refilling fast token bucket that immediately exits slow mode after a short time.
- **Approved D1 — slow first use and renewal:** one full slow exposure unit is available immediately upon the FIRST transition to slow (not a fresh unit on every later re-entry). Thereafter enforce a precise **rolling 12-hour consumption window**: for any decision at time t, the sum of committed slow exposure in (t−12 h, t] plus the proposed cost must be ≤1 full buy-in unit, including fractional funding and exposure across every table and tier. Each consumed fraction becomes available again only when its own 12 hours elapse; never replace the remainder with a new full unit at a fixed renewal boundary. Example: 0.4 units at t=0 and 0.6 at t=+1 h yield 0 available until t=+12 h, then 0.4 available, and at t=+13 h the full unit becomes available. No accumulated/catch-up units, no resets on switching tier/session/table or fast→slow→fast→slow. Store committed usage/receipt identities durably, enforce atomically across concurrent sessions and retain sufficient history for the rolling window. Fast remains the separate per-account fixed 7-day cadence described above; renewed fast does not undo old slow spends or reopen DRAINING. Renewing slow entitlement does not refill SYSTEM.
- These are access/exposure caps, NOT a prediction of winnings or a direct cap on payouts. Bot decision speed, hand timers, settled winnings and human-vs-human play never slow down.

## C. Chargeable exposure — required authoritative contract before implementation

- Account for **bot stacks newly made available for play against the player**, including bots already pre-funded at a CONTINUOUS_BOT/bot-only table when the human first joins. A new visitor must not repeatedly join previously funded bot-only tables and receive free unmetered bot exposure.
- Pure bot-vs-bot play **before any human admission charges no per-user allowance**. Count a bot exposure to a particular user only once for the same authoritative funding/admission identity; no extra charge on reconnect, reload, duplicate join, retry or simply replaying another hand against the same already-accounted funded bot stack.
- New bot seed, replacement and managed top-up for a human-eligible table must pass budget authorization before becoming visible; charge only NEW incremental funding/exposure, not the whole replacement buy-in again when old residual stack is retained. Existing funded bot chips are never charged as newly minted CH.
- Multiple standard humans may sit together: independently authorize each player's exposure to bots on admission and on genuinely new funding; do not divide or attribute fungible escrow winnings by guessing which player's chips came from a bot. If full conservative per-user accounting harms ordinary multiplayer access, resolve and document a smaller *authoritatively provable* attribution before implementation rather than silently bypassing the cap.
- Reserve/consume budget and persist the authoritative join/rollover/funding decision atomically or by idempotent fail-closed reconciliation; a partial write must not issue chips, reset allowance or leave an invalid seat. Do not refund allowance merely because a bot loses or a player wins. Define any genuine reversal/refund only for rolled-back funding/admission that never became available.
- Distinguish **accessed new bot-funded exposure** (quota accounting) from **realized net bot-to-human subsidy** (reporting/refill). Record identity/version/source and preserve auditable proof through existing ledger retention/archives when needed.

## D. Server-enforced matchmaking: standard vs constrained

- Standard players (sufficient fast allowance for the required exposure) may share a suitable standard bot-funded table, including available managed bot-only tables. Constrained players (fast exhausted/insufficient) MUST NOT be seated with standard players in any NEW bot-funded table and MUST NOT join already-funded bot-only / CONTINUOUS_BOT tables.
- A constrained player may choose a **private slow-mode table: one human + that human's authorized slow-funded bots**, or human-only tables with other humans (including standard players, as no one consumes bot funding there). Constrained players cannot share another player's funded bots. Existing ordinary multiplayer preference remains available for standard players.
- Matchmaking/eligibility is authoritative on the server for lobby discovery, Quick Seat, Create Table, manual join, direct URL, reconnection and WS bootstrap; UI filtering alone does not protect funding. Prevent bypass by selecting a table directly, duplicating requests or switching tabs. Reject an inadmissible NEW join with a clear non-punitive explanation and compatible table alternatives.
- Existing bot-only managed tables are standard-class when eligible humans are admitted; pure bot-vs-bot background lifecycle may continue, but slow-mode users may never access their pre-funded stacks.
- A slow table may finance bots at most within its single player's slow entitlement AND actual protected slow SYSTEM liquidity. No human can occupy another player's slow bot table. If no bot funding is available, do not start or promise a bot-funded hand; offer human-only play/wait for the next entitlement without halting valid settlements.

## D.1 — Approved 2026-09-25: fail-fast, user-specific matchmaking and find-or-create

**User-facing invariant:** a player browsing the ordinary poker lobby must see only tables they can **currently** join under authoritative account/tier/budget/class/owner/capacity/lifecycle rules, plus their own legitimately rejoinable tables shown explicitly as `Resume` rather than as new join opportunities. Do NOT show forbidden/full/DRAINING/other-user SLOW_PRIVATE tables as clickable choices, disabled `Unavailable` rows, or a `Join` option that predictably leads to policy denial. A server-built per-recipient projection must filter before rendering; browser-side filtering alone is insufficient. Refresh the projection after relevant budget/eligibility changes, seat/capacity/class/drain changes, and time boundaries; do not expose another player's private budget or slow table.

**Quick Seat / ordinary 'Join' experience:** when the player asks to be seated at a given allowed tier/mode, first prefer a suitable existing shared STANDARD table with humans (when eligible), then other compatible STANDARD tables; SLOW_PRIVATE is owner-only and HUMAN_ONLY has no bot funding. If no compatible joinable table exists, **automatically create a new table of the requested/eligible class** and continue the seat flow using existing table-creation and authoritative WS admission mechanisms. Do not require the player to return to the lobby, repeatedly click Join, or manually create an ordinary eligible table because discovery returned no candidate. Do not silently switch the player's requested funded mode/tier to HUMAN_ONLY or SLOW_PRIVATE; present that choice when necessary. No duplicate table or bot-funding side effects on retry; avoid unbounded create/retry loops and orphan empty tables.

**Stale candidate / race:** discovery is advisory, not an irrevocable reservation. After a table was displayed or recommended, it may become full, DRAINING, incompatible, or lose the required budget or bot liquidity before authoritative admission. Keep the WS transactional authorization and direct-URL/manual-join protection. On a normal lobby/Quick Seat admission race, **transparently and with a bounded retry** re-run matching and, where allowed, find another table or create one; do not surface the first predictable stale-candidate rejection as the normal user outcome. If the player explicitly opens a direct deep link that cannot be joined, do not bypass policy or silently seat them at an unrelated table: show a clear, non-punitive alternative or offer the regular find-or-create flow. Return a user-facing terminal state only when no safe compatible funded table can actually be offered after bounded retries or the backend is unavailable.

**Real constraints are not errors to paper over:** creating a table is not permission to mint/fund a bot or consume a user's allowance without the normal atomic admission/funding checks. If the requested bot-funded mode is impossible because of insufficient fast/slow allowance, depleted protected SYSTEM pool or unavailable proof/capability, do not create/promote a table or promise a bot-funded hand that cannot be financed. Display an immediately usable HUMAN_ONLY choice or an accurate wait/next-eligibility indication when calculable. For a player who has exhausted FAST, offer their own existing SLOW_PRIVATE if rejoinable, otherwise create one only if the slow allowance and protected liquidity allow it; a HUMAN_ONLY request may find or create a human-only table regardless of FAST. An empty human-only table need not manufacture other humans. Legal rejoin/settlement and previously approved 30-minute DRAINING remain unchanged.

**Acceptance / fundamental verification:** (1) a constrained user never sees a joinable STANDARD/bot-only or another user's SLOW_PRIVATE; a standard user never sees an incompatible/full/DRAINING table as an available Join; existing seats are explicitly resumable; (2) eligible ordinary Quick Seat prefers compatible existing multiplayer and creates a matching table when none exists; eligible SLOW_PRIVATE/HUMAN_ONLY follow their own find-or-create flow; (3) a race between discovery and admission auto-reselects/creates without leaking forbidden admission, double charging or duplicate funded tables; (4) genuinely insufficient budget/liquidity/authorization yields a clear allowed alternative, not false funded-table creation. Limit automated tests to fundamental backend matchmaking/admission and race contracts; verify UI presentation through manual preview/smoke, not new UI/CSS/JSP rendering tests.

## D.2 — Approved 2026-09-25: admin table inspection is separate from player matchmaking

The fail-fast, user-specific discovery filter in D.1 applies ONLY to the ordinary player lobby, Quick Seat and admission suggestions; **it MUST NOT hide tables in the existing authenticated admin operations list/details**. An allowlisted admin needs read-only visibility of **all persisted poker tables** (including STANDARD, SLOW_PRIVATE, HUMAN_ONLY, managed bot-only, DRAINING and CLOSED when the admin chooses an all/closed status filter), irrespective of the admin account's own fast/slow budget, seat eligibility, slow-table owner, table capacity or the matchmaking filter. Preserve the existing bounded pagination/status filters and access checks; do not silently apply player-specific eligibility to `admin-tables-list` or `admin-table-details`. Process-local guest tables, if not persisted in the existing admin inventory, are not silently implied to be part of this list.

Extend the EXISTING `netlify/functions/admin-tables-list.mjs::listTables`, `netlify/functions/admin-table-details.mjs::loadTableDetails` and `js/admin-page.js` table-list/detail rendering with the minimal **read-only** information necessary to inspect policy: `bot_access_class` (STANDARD / SLOW_PRIVATE / HUMAN_ONLY, or explicit LEGACY/unknown), DRAINING active state, `bot_draining_started_at` and `bot_draining_deadline_at` when applicable. Keep `poker_tables.status` OPEN/CLOSED separate from the DRAINING policy state. Use the persisted policy fields and existing authorized admin patterns; do not infer a class from number of bots or accidentally hide a missing/legacy classification. Include actual access-class/drain state in the admin details even when a table is not joinable to its administrator. Do not show players' private hole cards, secrets or another account's allowance. Only essential auth/projection tests; presentation verified by normal preview/manual smoke (no UI/CSS rendering test suite).

**No admin join bypass:** admin-only read/inspect privilege does NOT grant automatic admission, an extra seat, gameplay actions, bot funding or an exemption from FAST/SLOW constraints. An admin joining to PLAY remains subject to ordinary table/seat/budget rules. Admin inspection must not mutate gameplay, escrow, allowance or lifecycle. A live read-only table spectator view is a SEPARATE feature already tracked by [#789 — Poker: Spectator Mode](https://github.com/krzysztofcal/arcadePlatform/issues/789), not an implementation requirement of #869. #789 is CLOSED and its body describes a V1 concept, so its implementation status should be verified separately; do not infer a working live spectator from the existing admin table details. Related #835 covers only possible spectator reaction support. Neither #789 nor #835 should be silently implemented under #869.

**Acceptance:** (a) ordinary FAST/SLOW users continue seeing only their own compatible joinable/rejoinable choices under D.1; (b) authenticated admin sees every persisted table across the above classes and statuses in the operations list/details, including those invisible to that admin's player lobby; (c) class, DRAINING state and original deadline are visible accurately without changing the game's OPEN/CLOSED status; (d) non-admin cannot access this inventory; (e) inspecting a table cannot join, fund, change seats, initiate a hand, expose private cards or bypass matchmaking.

## D.3 — Approved 2026-09-25: bounded Supabase load, egress and gameplay availability (mandatory before implementation)

**Motivation / release gate.** The per-player lobby projection, multi-table budget/drain checks, receipt journal, and refill could cause query fanout, high DB CPU/Disk I/O, lock contention and response egress as the number of players/tables grows. Earlier #735 (egress), #742 (wasteful poker queries) and #962 (Stage maintenance overload) show why an indexed query alone is not proof of safe total workload. **No new economic admission, funding or refund invariant may be weakened as a performance shortcut.** Record a reviewable bounded cost model and Stage measurements before authorizing Production activation; do not invent unmeasured numeric performance promises.

1. **Personalized lobby: no DB query per player × table.** In `ws-server/server.mjs::buildLobbySnapshotPayload/sendLobbySnapshot/maybeBroadcastLobbySnapshot`, preserve the existing WS-authoritative active-table registry as the primary source of public table metadata. Resolve each recipient's private eligibility/budget with a bounded, deduplicated read per recipient or meaningful eligibility change, not a fresh Supabase read for every candidate or every subscriber on every table update/1-second visibility sweep. Apply lightweight server-side filtering to a bounded candidate set; never broadcast a full private budget or foreign SLOW_PRIVATE metadata. Identify which facts can be reused from the currently authoritative WS state and which require fresh DB proof; cached/stale proof may omit an offer but must **never** authorize a new seat, funding or claim that funds are reserved. Do not add a second independent cache of live poker state. Keep admin list/details outside player-lobby filtering per D.2.
2. **Coalesce and bound invalidations/fanout.** Budget, seat, drain, pool/proof/capability, time-boundary, reconnect and table-change events must not trigger one full SQL scan or per-table DB lookup for every connected lobby subscriber. Reuse the existing lobby broadcast/sweep; coalesce duplicate refreshes, avoid overlapping recipient queries, discard superseded async results, and apply bounded pagination/projection size and payload fields. Preserve prompt correctness at FAST/SLOW transitions and when a table becomes full/DRAINING; use a fresh authoritative final admission check to close discovery races. Define a bounded update policy rather than a new high-frequency per-user poll. A proof/DB failure omits new JOIN suggestions with an honest temporary-unavailable/recovery state; it must not create a storm of retries, leak inaccessible tables, or spuriously report zero candidates as proof that create is safe.
3. **Bounded find-or-create and operation journal.** In `netlify/functions/poker-quick-seat.mjs::selectCandidate/recommendSeatAtTable/handler`, filter cheap SQL predicates before LIMIT and use existing bounded keyset/continuation for any snapshot-dependent candidate checks; stop after an explicit, measured work/time budget without interpreting an incomplete search as no matching table. Never run unbounded `select *`/full-table scans, per-candidate full ledger scans, or create a duplicate table because a page/read was cut short. Keep D.1's max 2 admission attempts / 1 create and durable replay; bound concurrent HTTP operations per account and repeated stale/unknown-result recovery without busy retry. At final WS join/rollover, use index-backed per-account/table access and receipts with an explicit upper bound on work/lock hold time; do not re-scan all ledger history or all poker tables per join, reconnect, bot action or hand. Where a full authoritative check cannot complete within the safe budget, deny/defer NEW funding/admission; never approximate the accounting result or release ledger/seat locks early.
4. **Refill is low-priority, bounded maintenance, not an on-demand gameplay dependency.** In `shared/poker-domain/bot-bankroll.mjs`, `ws-server/server.mjs` and existing janitor/sweep, coalesce duplicate close-proof triggers, cap per-cycle candidates, concurrency, retry/backoff and maximum DB work; do not spawn an independent heavy refiller for every close or re-scan every open escrow/entire historical ledger on every tick. Preserve the exact global/tier/class rolling-168h caps, verified signed realized loss, complete active-capital/headroom proof and atomic emission receipts; design a bounded/indexed or transactionally maintained read of **complete** required evidence, with measured plans and no unsafe partial sample, stale economic cache or silent cap bypass. If exact complete proof or database capacity is unavailable, emit **zero** new CH and leave the durable proof pending for later bounded reconciliation. Run refill only after terminal commit and outside live-hand, player cash-out and table accounting locks. Refiller/sweep must not monopolize DB connections, lock shared resources for an unbounded time, or starve join, ongoing hand settlement, legal cash-out and safe table close. Maintain a protected operational path for settlement/cash-out even when NEW bot funding, matchmaking or refill is temporarily deferred; do not promise continued game availability if Supabase itself is fully unavailable.
5. **Explicit resource and query budgets, existing tooling only.** In `plan.md` and targeted `tasks.md`, give each added hot path (lobby subscriber update, Quick Seat, join, start/prepare/commit rollover, drain fanout, close, refill/sweep) a concise cost model: DB reads/writes/round trips per invocation, invocation frequency, rows/bytes returned, indexed access/required query plans, lock order/duration and bounded concurrency/retry. Identify shared global contention points (SYSTEM pool and issuance guard), preserve existing lock order, and confirm critical poker action/settlement remains independent from heavy full-history/proof scans. Prefer existing SQL indexes, narrow projections, WS registry and existing coalescing/metrics; no new caching framework, polling service or telemetry database solely for D.3. Measure Supabase response egress **separately** from DB CPU, Disk I/O, WAL and connection/lock pressure; a low egress reading alone does not establish safety.
6. **Stage evidence and fail-safe rollout.** Extend the existing `quickstart.md` Stage/WS Preview gate with bounded before/after measurements for normal idle, multiple connected lobby viewers/tables, concurrent join/rematch, busy hands/rollover, multi-table exhaustion/drain, simultaneous terminal closes/refill and recovery/backlog. Capture approximate query/transaction count and payload bytes per path, Supabase egress and CPU/Disk I/O, DB connections/lock waits/serialization retries, lobby update latency, join/hand-settlement/cash-out latency and cleanup backlog. Use small, explicitly bounded synthetic load on isolated Stage/Preview, never unrestricted load on Production or saturating shared Stage. Compare against the current baseline and document bottlenecks, failure behavior and concrete conservative concurrency/page/batch limits justified by observations, not arbitrary SLOs. If DB health is critical/unknown, fail closed for optional destructive maintenance/refill and stop expanding load; preserve already accepted hands/legitimate financial settlement as far as the existing database allows. Existing Stage resource guards in #962 must not be bypassed. Do not declare #869 implementation merge-/Production-ready from green unit tests or Netlify preview alone: exact-SHA WS Preview Deploy and manual preview/stage smoke are required, followed by separate Production GO for new supply/refill and activation.

**Acceptance / essential verification:** One lobby change cannot produce DB queries proportional to `connected_viewers × visible_tables`; an unchanged idle lobby must not add a new per-viewer high-frequency DB poll. Candidate search, fanout and refill must have explicit bounded work and recoverable backlog without unbounded retry or duplicate create/mint. At least one fundamental backend/runtime verification exercises coalesced recipient refresh and bounded failure/backpressure; transaction-level scenarios verify admission/settlement correctness under contention and zero refill if full proof is unavailable. Stage measurements must show the actual cost at the tested scale and no material starvation of accepted hands, legal cash-out or admin access. Performance optimization must never make an ineligible JOIN visible as authoritative or bypass final funding checks.

## E. Player exhausts fast budget while already seated — 30-minute draining, not a kick

- Current live hand and legitimate payouts must finish unchanged. Do **not** remove the user, auto-transfer their stack, change their hand rules or force instant cash-out.
- **Approved P1 — player-global fast exhaustion / all currently occupied standard bot tables:** the first authoritative committed fast exhaustion event (exactly zero after approved funding, or first denial for insufficient fast on required new bot exposure) belongs to the PLAYER and has one durable timestamp per fast period; it is not merely a flag on the table where funding was requested. Every STANDARD bot-funded table at which that player is ALREADY seated must enter sticky DRAINING with deadline = the **original player exhaustion timestamp + 30 minutes**, including tables with only pre-funded bots and NO immediate new funding request. Propagate the change promptly to all their occupied STANDARD bot tables; missing/delayed propagation must NOT reset the deadline. On every seat/admission, existing-table check, new bot funding decision and every start-hand/rollover prepare AND commit, authoritative server must re-read/reconcile the players' durable global fast state and reject new SYSTEM funding/admission when any seated player's exhaustion applies. A table discovered later inherits the ORIGINAL timestamp/deadline, never a fresh 30-minute grace. Detect exhaustion even if a particular table does not need a replacement bot. Handle multi-table events concurrently without deadlocks/partial financial commits. The triggering funding that legally consumed the last fast allowance may commit; no subsequent new funding at any affected table. A later fast period does not undo already sticky drain. HUMAN_ONLY and existing SLOW_PRIVATE tables are not drained solely due to fast exhaustion. Preserve current legal hands, seated humans, stacks and cash-out; no automatic forced move or mid-hand kick. This is the explicit temporary exception for previously co-seated standard and newly constrained players.
- While DRAINING: **no new human admissions and NO NEW SYSTEM funding of any bot** at that table (no seed/replacement/managed top-up); existing already-funded bots may finish normal hands from remaining stacks. Other humans may leave voluntarily and use new compatible tables without forfeiting cash-out. Do not hold another player's existing live hand or payout hostage to the exhausted user's allowance.
- When deadline is reached, block starting the **next** hand; if a hand is live, settle it first. Then close using existing terminal accounting/zero-escrow protections, cash out humans and return bots' residuals to exactly proven SYSTEM sources. If all humans leave earlier, permit existing safe lifecycle close. Never start a new bot-funded hand beyond the deadline; don't forcibly terminate a live hand at the 30-minute mark.
- Existing CONTINUOUS_BOT tables have rotation_due_at, but handleContinuousBotRotationAtSettled currently POSTPONES when a human remains. Draining deadline must be a separate non-postponable bound/override; ordinary managed rotation and unrelated standard table TTL need not be rewritten. Any other standard table carrying the fast-vs-slow hazard must also honor DRAINING. 30 minutes is a **maximum admission/next-hand grace** after exhaustion, not a universal TTL for all poker tables.
- Resolve failures/restarts, absent players and retry idempotency deterministically. A funding failure at either 100 or 500 CH must not create an infinite settled-rollover retry while a valid hand/exit could progress; reuse existing controlled no-new-bot-funding path where safe.

## Approved D2 — exact weekly issuance period

For refill only (not per-user fast cadence), enforce each tier, class (STANDARD/SLOW), and global issuance ceiling in **every rolling 168-hour period**, using durable UTC issuance receipts and transactional/serializable per-scope guard. At time t count previous committed REFILL emissions with timestamps in (t−168 h, t], plus the proposed REFILL amount. No shared fixed-week reset, no double issuance across adjacent calendar-week boundaries, no in-process-only counters, and no pruning of required receipt/idempotency evidence. One-time INITIAL_ALLOCATION is audited separately from the REFILL ceilings. A tier/class/global concurrent refill must hold the requisite shared guards atomically.

## F. Real tier bankrolls and automatic bounded refill (approved initial 100/500 policy)

| Bot buy-in | Target real tier bankroll | Maximum NEW CH issued to that tier per 7 days | Initial slow-class reservation of available bot liquidity/refill |
| --- | ---: | ---: | ---: |
| 100 CH | 1,000,000 CH | 100,000 CH | 10% (10,000 CH of weekly issuance ceiling) |
| 500 CH | 1,000,000 CH | 500,000 CH | 10% (50,000 CH of weekly issuance ceiling) |
| Total current tiers | 2,000,000 CH target | **600,000 CH / 7 days global ceiling** | **60,000 CH / 7 days reserved for slow-class issuance** |

- Real bankroll for 100 CH MUST be isolated from general TREASURY. Keep existing POKER_BOT_BANKROLL for 500 CH and preserve immutable provenance of already-financed bots. Provision/seed the 100 CH dedicated bankroll **once**, with explicit Stage evidence and separate Production approval, recognizing that a new 1,000,000 CH SYSTEM seed is a one-time economy/supply change. Choose and document its exact balanced funding transfer/source in the implementation preflight; no accidental duplicate allocation, no retroactive change to funding for already-active tables. The one-time initial allocation is separate from weekly refill quotas; require owner GO before Production issuance.
- At any trigger, refill only actual settled net loss to humans, not the temporary lower liquid balance caused by bot funds sitting in an OPEN table's ESCROW. Determine durable attributable *realized* shortfall from committed bot funding minus proven terminal returns on safely closed/reconciled tables, accounting for already refilled amounts, ledger retention/archives and any still-live exposure. Do not infer emission from total table turnover, raw live balance, or generic TABLE_CASH_OUT metadata alone.
- Allowed refill amount is the smallest of: (i) verified uncompensated realized loss, (ii) headroom to that tier's target real capitalization without counting active committed stacks as lost, and (iii) remaining per-tier AND total global weekly issuance ceilings. Zero if any condition is not provably satisfied. Never pre-mint full weekly maximum just because a new week starts.
- Refill uses the existing append-only double-entry ledger as a bounded, idempotent, auditable **MINT/GENESIS -> exact tier SYSTEM account** transaction, only after explicitly approved funding policy/controls. Persist the weekly counter, per-source purpose, funding identity, and idempotency key in one locked/serializable transaction with the ledger credit. An error, race, retry or restart cannot issue a second credit or exceed a ceiling. A SYSTEM-to-SYSTEM internal rebalance is not new emission and must not be counted as newly minted CH.
- Actually reserve **10% of available tier bot funding AND of its permitted weekly refill for slow mode**, with **90% reserved for standard mode**. Preserve the allocation against simultaneous requests with transactional enforcement, whether by the smallest safe existing-ledger-compatible class-specific SYSTEM subaccounts or equivalent truly enforced reserved accounting. A soft matching preference or an unprotected one-account balance check is NOT an actual reserve. No cross-class or cross-tier fallback when a protected quota is empty; unused slow quota may remain unused, not be silently diverted to standard users.
- A slow player may use their protected portion only within their own slow allowance. Fast/standard players use the standard portion. Neither portion's entitlement is a direct payment to USER; actual chips move to bots' table escrow on a permitted seed/replacement/top-up.
- If global issuance cap or protected real capital runs out, new bot funding pauses safely in the affected class/tier; an ordinary player may temporarily find no bot seat. **Finite issuance and guaranteed unlimited bot availability for an arbitrarily large number of accounts are incompatible**; never promise both. Existing hands/legitimate payout remain operational.
- Initial values are approved **pilot caps**, not validated inflation/supply predictions. Stage must measure realized outflow, ordinary human access and consumption; any Production activation/refill requires a separate explicit GO. For #870, set tier-specific capitalization and global issuance ceilings for each higher tier, with an updated cross-tier global CH cap BEFORE enabling its bots; no automatic extrapolation from 100/500.

### Approved D3 — one-time initial capitalization of 100 CH bot fund

Fund `POKER_BOT_BANKROLL_100` **once** by a purpose-unique, idempotent, auditable **1,000,000 CH GENESIS → POKER_BOT_BANKROLL_100 MINT**; reserve 900,000 CH for STANDARD and 100,000 CH for SLOW. This is genuine one-time new CH issuance, distinct from weekly REFILL limits (which do not authorize an automatic second seed). Provisioning the account/schema does not itself mint CH. The actual seed, its exact Stage/Production target and Production supply impact require verification, Stage evidence and a separate explicit Production GO; keep old TREASURY bot provenance and the historical 500 pool unchanged for existing stacks.

## G. Speckit/Codex implementation workstreams — minimum existing architecture

**T1 — evidence & invariant contract (read-only).** Review agents.md and skills.md; use current GitHub source, not historical repomix by default. In shared/poker-domain/table-economy.mjs, shared/poker-domain/bots.mjs (seedBotsForJoin), shared/poker-domain/join.mjs (executePokerJoinAuthoritative), ws-server/poker/table/table-manager.mjs (prepareSettledHandRollover, commitSettledHandRollover), ws-server/poker/persistence/persisted-state-writer.mjs (writeReplacementFundings/writeManagedBotTopUps/writeMutation), shared/poker-domain/terminal-close.mjs (resolveBotFundingSource, executeTerminalPokerCloseInTx), and ws-server/server.mjs map existing source, cash-out, funded seats, retry and settlement. Validate 30-min drain semantics and closed-table net-loss measurements on Stage. Record the unavoidable existing-human transition exception and explicit breaking change to standard/slow admissions. Do not invent escrow ownership.

**T2 — persistent budget + atomic admission.** Add the smallest forward-only Supabase migration under supabase/migrations/ for per-user 7-day fast usage, 12-hour slow entitlement, idempotent exposure identity and table class/drain state ONLY where existing tables cannot safely hold them. Keep durable user identity and table identity server-side. Extend shared/poker-domain/join.mjs executePokerJoinAuthoritative and shared/poker-domain/bots.mjs seedBotsForJoin so pre-funded bot-only admission and new seed are charged correctly in the same authoritative funding/admission transaction. Integrate per-user checks into ws-server/server.mjs and ws-server/poker/persistence/persisted-state-writer.mjs funding rollover; no second poker engine or generic fraud service. Any Stage migration PR automatically mutates shared Stage via DB Stage Apply PR: explicitly declare/review migration effect before PR; applied Stage migrations are immutable; Production remains separate GO.

**T3 — class-safe table selection and separate admin inspection.** Extend netlify/functions/poker-quick-seat.mjs selectCandidate/handler, the existing Create Table flow found in repo, ws-server/server.mjs and shared/poker-domain/join.mjs to enforce STANDARD, SLOW_PRIVATE and HUMAN_ONLY admission and deny bypass by direct join. Implement the approved section D.1 fail-fast personalized lobby, find-or-create Quick Seat and bounded re-match after stale admission; verify no predictable incompatible Join UI and no false funded-table creation. Reuse existing lobby and table lifecycle, do not build a generic load balancer. STANDARD allows multiple eligible humans; SLOW_PRIVATE one human + exclusively authorized slow bots; HUMAN_ONLY normal human multiplayer. Ensure an existing incompatible table is never silently relabeled or joined after deployment. Update existing admin-tables-list/admin-table-details/admin-page read-only projections for class/drain/deadline and explicitly exempt admin inventory from player-lobby filtering; do NOT grant admin gameplay admission bypass or implement spectator-mode #789 here.

**T4 — drain without interrupting hand.** Reuse ws-server/poker/runtime/continuous-bot-table-rotation.mjs handleContinuousBotRotationAtSettled, ws-server/poker/persistence/continuous-bot-table-repository.mjs, ws-server/poker/table/table-manager.mjs prepareSettledHandRollover/commitSettledHandRollover, ws-server/server.mjs runSettledRolloverCommand, ws-server/poker/runtime/table-janitor.mjs and shared/poker-domain/terminal-close.mjs. Add a durable non-postponable table draining deadline, prevent NEW funding/admissions, allow funded stacks/live hands until safe 30-minute deadline close and preserve all payouts. Keep ordinary managed table rotation and unrelated standard tables unchanged.

**T5 — real tier pool, protection and refill.** Reuse shared/poker-domain/table-economy.mjs getBotFundingSystemKeyForBuyIn with class-aware selection as required, shared/poker-domain/bots.mjs, persisted-state-writer.mjs and terminal-close.mjs funding provenance, and netlify/functions/_shared/chips-ledger.mjs postTransaction; use a new forward-only Supabase migration for necessary tier/class funding accounts and persistent issuance counters. Derive approved net realized shortfall from ledger-authoritative closed-table accounting, not OPEN escrow, and protect 90/10 allocations transactionally. Implement idempotent bounded refill and one-time protected 100 CH seed. Preserve 500 CH source provenance for pre-existing bots and do not alter existing seed migration. Runtime must remain fail-closed if recap/archived proof is missing.

**T6 — fundamental deterministic tests + bounded-load evidence + release gates.** Extend existing shared/poker-domain/*.behavior.test.mjs, ws-server/server.behavior.test.mjs and ws-server/poker/persistence/persisted-state-writer.behavior.test.mjs (or the nearest EXISTING contract tests) only for key rules: standard vs slow/private/manual/direct/bot-only join, pre-funded bot exposure, multi-user budget, cross-tier/session/retry, exhaustion mid-hand then 30-minute safe close, no bot funding while draining, terminal cash-out provenance, concurrent refill/idempotency/global ceiling and live-escrow-not-loss. No broad speculative/UI/CSS tests. Exact runtime SHA WS Preview Deploy + Stage/Preview manual smoke for both classes/TTL, and explicit separate Production approval. Existing bot-only Stage churn must not consume a human budget or trigger inflation.

## Notes / guardrails

- Use deep reasoning but produce the smallest condensed code, refactor before review; prefer existing packages/classes/methods; do not write entire implementation into the plan. Avoid git commands.
- Explicitly highlight breaking changes to lobby/Quick Seat, direct URL admission, mid-table lifecycle, 100 CH TREASURY sourcing, immutable provenance and financial supply/issuance. Keep gameplay hand rules unchanged.
- All added browser JS must operate on JSP; each CSS selector gets exactly one line, with no line breaks inside declarations.
- If adding any inline script, add its correct SHA to the CSP allowlist; prefer not to add scripts or CSS unless truly required.
- Use klog, never console.log. Only fundamental deterministic critical backend/runtime tests. Validate all states and error paths; Production changes only on explicit approval.
- #870 currently documents NO automatic refill for higher tiers. Before #870 implementation, reconcile that text explicitly with this APPROVED bounded-refill policy; it is NOT permission to enable higher tiers, mint arbitrary CH, or change existing Production behavior.

## Acceptance criteria

- Ordinary users can share STANDARD bot tables; constrained users cannot enter them or pre-funded bot-only tables, and can access HUMAN_ONLY or their isolated slow-bot table when both allowance and real slow liquidity permit. All server join paths enforce this.
- Fail-fast matchmaking: the personalized lobby shows only currently joinable choices for the viewer (plus clearly marked legitimate Resume seats), never predictable policy-denied/full/DRAINING Join buttons; eligible Quick Seat finds an existing matching table or creates a compatible one when none exists. Bounded race retry transparently re-matches after stale candidates, without bypassing authoritative admission, double funding or silently changing the player's requested mode. Genuine lack of allowance/liquidity is communicated with human-only/wait alternatives instead of false funded-table creation.
- Admin inventory remains authorized and unfiltered by player matchmaking: all persisted STANDARD/SLOW_PRIVATE/HUMAN_ONLY/managed/DRAINING tables remain inspectable with class, drain state and original deadline, including CLOSED when selected. Admin inspection is read-only; no privileged join/seat/budget bypass or private-card exposure. Live spectator mode is separately tracked in #789.
- Persisted 100-units/7d fast and 1-unit/12h slow (burst one) function across all tiers and sessions. Exhaustion is not a punitive classification. Legal winnings, existing hands and cash-outs always settle.
- A currently seated user exhausting fast allowance remains for at most the 30-minute draining grace with no newly funded bots or new joiners; live hands settle before close and cash-out is invariant-safe. Ordinary users elsewhere remain unaffected by that user's depleted allowance.
- Player-global exhaustion while seated at tables A and B drains BOTH standard bot tables by the same original t+30min deadline, even when B has only pre-funded bot stacks and no pending funding. No fast-session/table switch restores normal admission. Human-only/slow-private existing tables do not drain on that cause.
- Rolling slow consumption under concurrent partial funding enforces sum(usage in prior 12h + proposed) ≤1 unit; a fraction restores only 12h after its own use, with no extra first-unit on fast/slow transitions. Rolling 168h issuance cap holds across time-window boundaries and across classes/tiers.
- 100 CH bots no longer drain general TREASURY after a verified migration; 100/500 SYSTEM capitalization and true 90/10 class protection are maintained, with **at most 100k+500k CH of NEW issuance per 7 days across current tiers**, independent of transient live escrow. Refills are audited, idempotent and bounded; missing evidence means no refill.
- No claim of guaranteed infinite availability when global budget is exhausted; bot funding suspends gracefully without stalling existing hand or legal payout.
- Only fundamental deterministic tests and verified exact-SHA WS Preview/Stage smoke before any separately approved Production rollout.

## Spec Kit status and next handoff (2026-09-25)

- **NEW owner-approved D.3 (2026-09-25):** before any implementation authorization, update the SAME docs-only draft PR #1017 with the bounded-query/egress/CPU-IO/lock-risk requirements and Stage evidence gate above; run `$speckit-analyze` and obtain independent review. This scope extends the ongoing D.2/P1/P2 correction, but is NOT permission for code, migrations, deployment or Production mint/refill.

- [Draft PR #1017](https://github.com/krzysztofcal/arcadePlatform/pull/1017) remains DRAFT and docs-only at the last reviewed SHA `477e88228fa1028bdaaae7365528e4d1191153ad`. Prior Spec Kit corrections D1–D3, two earlier P1 findings, and separate FUNDING/EXPOSURE have been independently reviewed; no code or migrations have been approved or executed as part of planning.
- The owner additionally approved the fail-fast discovery and automatic find-or-create + bounded stale-candidate retry contract in section D.1 on 2026-09-25. **This is a NEW Spec Kit requirement; the existing PR documents do not yet fully implement it.**
- The owner additionally approved section D.2: separate complete, read-only authenticated admin inventory with class/DRAINING/deadline, not filtered by player eligibility. Existing [#789](https://github.com/krzysztofcal/arcadePlatform/issues/789) covers a separate live spectator concept and has been REOPENED; no duplicate issue was created. **Astra should integrate D.2 into the same draft Spec Kit PR while its D.1 work is in progress, before independent review; do not add admin gameplay bypass or full live spectator implementation to #869.**
- **Next: Astra updates the SAME draft PR #1017** using the repo's Spec Kit flow: re-read issue and current source, clarify/specify the D1 acceptance scenarios, update plan/contracts/tasks/checklist and only essential fundamental tests, rerun `$speckit-analyze`, publish updated docs only, and STOP for independent ChatGPT review. Do not invoke `$speckit-implement` or delegate to Luna before the reviewed plan receives an explicit separate implementation assignment. Publishing documents is not authorization for Stage/Production migrations, seed/refill or deployment.

## Related

#788 progression; PR #868 current bankroll model; #783 continuous bot tables; #870 higher-tier bot liquidity after #869. [#789 Poker: Spectator Mode](https://github.com/krzysztofcal/arcadePlatform/issues/789) — separate read-only live spectating concept (OPEN; implementation to verify); #835 spectator reactions (separate follow-up).
