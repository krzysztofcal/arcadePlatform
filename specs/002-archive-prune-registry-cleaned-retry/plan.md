# Implementation Plan: Archive prune registry-cleaned retry

**Branch**: `002-archive-prune-registry-cleaned-retry` | **Date**: 2026-09-12 | **Spec**: [spec.md](spec.md)

## Summary

Patch only the registry predicate in the complete prune receipt branch of `public.chips_prune_committed_archive_batch_internal(text,uuid[],bigint[],boolean)`. Read its effective definition with `pg_get_functiondef`; require exactly one expected predicate before replacement. Reuse the established owner-role migration pattern. Preserve all other text and function properties.

## Technical Context

PostgreSQL 17 / PL/pgSQL on Supabase; existing Node assert/postgres migration harness. No new dependencies. Scope: one forward migration and additions to `tests/chips/chips.migration.test.mjs`. No additional queries in retry; evaluate the already loaded receipt and counts. Exact predicate: [contracts/prune-retry.md](contracts/prune-retry.md).

## Constitution Check

Pre-research and post-design: PASS. Database retention layer only; live GitHub main is authoritative. Reuses immutable receipt and least-privilege pruner; preserves runtime ownership and Stage/Production separation. Deterministic fundamental tests only. No browser JS, logging, CSS, CSP, configuration, tooling, ignore or dependency edits. No merge or historical spec changes.

## Project Structure

- `supabase/migrations/`: one new `chips_archive_prune_registry_cleaned_retry` forward migration, generated by existing CLI.
- `tests/chips/chips.migration.test.mjs`: extend existing serializable rollback pruning probe with authorized retirement and invalid-state savepoints.
- `specs/002-archive-prune-registry-cleaned-retry/`: specification, checklist, research, data model, contract, quickstart and tasks.

## Implementation and Validation

1. Establish the regression before migration using the existing pruning probe.
2. Patch the current predicate only, retaining wrong/extra mapping and hot ledger checks outside the alternative. Absent cleanup requires full mappings; any present receipt requires exact canonical Stage/v1 policy, all fields, count equality, valid digest, committed timestamp and zero registry/matching counts. Use `IS NOT TRUE` to reject NULL evidence.
3. Run the disposable PostgreSQL migration harness and existing fundamental pruning/bot-retention/automation tests. Compare effective definitions before/after with only the intended predicate normalized; inspect ownership/ACLs with existing tests.
4. Review and simplify the full diff, prepare draft PR linked to #981. Record CI application and exact existing-30d operational verification separately; use existing authorized gates only.

## Complexity Tracking

No violations or new abstraction. No full copy of the large function and no JS skip workaround.
