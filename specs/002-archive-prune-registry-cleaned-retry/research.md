# Research

- Decision: patch the effective function from the catalog. Its last full replacement is `20260904170000_chips_ledger_closed_human_prune_whitelist.sql`; earlier hardening adds strictness, target checks and least-privilege ownership. Later migrations retain this implementation. Copying the original August pruner would lose closed-human USER/ESCROW eligibility and aggregate handling.
- Evidence: run #803 at main `7a23aa08` logged healthy Stage at 02:09:49 UTC and existing-30d `P0001: Archive already-pruned state is inconsistent` at 02:09:56 UTC. Logs do not expose the failing batch ID. PR #980 records authorized batch 10 retirement, four deleted keys, complete receipt and zero mappings in run 34620142707. This provides the concrete legal state that the old retry predicate rejects; disposable reproduction is required.
- Contract: `20260911100000_chips_ledger_missing_table_bot_registry_retirement.sql` validates the exact Stage/v1 policy, immutable proof/prune receipts, whole identity set/count/hash, absent tables, bot identities and active TABLE fence before atomic deletion and receipt write. Its guard preserves closed-human GO. No independent original key digest exists in v1 after deletion; trust the protected receipt rather than fabricate a hash comparison.
- Alternatives rejected: JS skip (hides database bug), broadly accept absent registry (hides data loss), generalize policies (outside contract), copy old function (loses later guards).
- PostgreSQL `CREATE OR REPLACE FUNCTION` preserves owner and permissions; catalog extraction preserves declared attributes: https://www.postgresql.org/docs/current/sql-createfunction.html. Reuse hosted owner-role grant/reset/revoke pattern.
