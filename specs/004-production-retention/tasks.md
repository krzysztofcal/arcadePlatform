# Tasks: Production retention (#891)

**Input**: spec.md, research.md, migration-inventory.md, plan.md, data-model.md, quickstart.md in this directory.
**Status**: All implementation/operational tasks intentionally unchecked. Planning is complete, not implementation.
**Rules**: No merge by agent. Production tasks explicitly labelled OWNER GATE are not authorized by this document. No Production fixtures, cleanup or migration application without separate exact authorization. No blanket db push, migration repair, ignore/dependency cleanup, broad UI/DOM/browser tests.

## Phase 1 — authoritative refresh and inventory (PR A, US1)

- [ ] T001 [US1] Refresh live issue891/main and independently read both project/system identities, complete migration histories, columns/functions/ACLs, Production archive/registry/accounting aggregates and deployed WS/Netlify revisions. Update `specs/004-production-retention/research.md` and `migration-inventory.md`; compare against main `f7983d78333b51a393c0e9a6d3dfe48ce1224c74`. Expected: all main migrations categorized, no unknown remote version, exact Production `otbqfijerkieoxwpxjnm/7575202818581710058`, Stage `krydukthwdvccggbyjfw/7656985631720456337`; unknown drift stops milestone. Dependency: none. Read-only only.
- [ ] T002 [US1] Add `supabase/production-migrations/manifest.json` with first54 baseline, all43 source file/version/SHA256/category/required-vs-omitted object effects and replacement E1/E2 mapping. Extend `scripts/check-db-migrations.mjs` filename/hash/exhaustiveness validation for this directory without SQL execution or change to `scripts/stage-db-migrate.mjs`. Expected: historical Stage files immutable, intentional Production gaps explicit; one unclassified new migration/hash drift fails. Dependency: T001. Fundamental coverage: inventory/history fixture in `tests/chips/chips.migration.test.mjs` (T005), no new guard suite.

## Phase 2 — Production-equivalent contract (PR A, US1)

- [ ] T003 [US1] Author E1 `supabase/production-migrations/20260914090000_chips_ledger_production_retention_contract.sql`: atomic identity preflight before DDL; add final archive source/proof/GO/registry/account-retirement fields, registry table identity fields, poker bot/human markers, OFF TABLE fence, three Production singleton policies and `chips_production_retention_control`. Use data-model.md field defaults: enabled=false, max_transactions=2, all canary/activation fields null, historical and pre-fence bot eligibility false. Include only final inventory indexes; omit rolled-back index/seqscan hint and all legacy proof/run schema. Preserve old manual batch1 and ledger/account/registry values. Dependency: T002; append required final routines in T004 before treating E1 as complete.
- [ ] T004 [US1] Complete E1 final `chips_normalize_table_metadata`, `chips_table_transaction_before_insert`, `chips_validate_table_transaction_binding`, `chips_capture_transaction_idempotency`, `chips_guard_idempotency_mutations`, `chips_guard_poker_table_mutations`, `chips_guard_archive_batch_mutations`, `chips_register_archive_id_proof`, `chips_prune_committed_archive_batch`/`_internal`, bot scoped proof/lifecycle/authorize/prune/automatic routines, human lifecycle/authorize/prune/completion/automatic routines and Production escrow counterparts. Add `chips_authorize_production_existing_30d_canary` and `chips.production_existing_30d_go` immutable owner-gated transition as data-model.md, `chips_assert_production_retention_control`, `chips_set_production_retention_enabled`, `chips_lock_table_fence_for_retention`; preserve final owners/SECURITY DEFINER/search_path/RLS/API denials, complete human GO, #981 receipt retry and cap2 at every destructive boundary. Bind Production policies and canonical identity; omit Stage legacy and missing-table retirement API. Record only E1's real version atomically, no history repair. Dependency: T003. Expected: API latch spoofing, mixed target, unknown policy, incomplete proof or >2 tx cannot mutate.
- [ ] T005 [US1] Extend `tests/chips/chips.migration.test.mjs` with a separate disposable first54+E1 schema fixture and catalog/privilege/behavior checks. Compare required final schema to reviewed Stage definitions using only explicit target/policy/routine-name normalization; verify intentional omissions/indices, OFF defaults, preserved rows and history gaps, null-policy cap2 and all policy cap gates. Use test-only identity substitution confined to disposable fixture; never weaken shipped identity SQL. Extend existing `.github/workflows/tests.yml` migration-contract step to run that fixture on its current Postgres service; no new remote secrets or test framework. Dependency: T004. Expected: real PostgreSQL tests execute rather than skip; catalog match alone insufficient.
- [ ] T006 [US1] Author E2 `supabase/production-migrations/20260914091000_chips_ledger_production_table_fence_activation.sql` and add its hash/prerequisites to manifest. Require exact Production pair, E1/catalog/control OFF, session owner confirmation `chips.production_fence_confirmation` matching `ACTIVATE TABLE FENCE otbqfijerkieoxwpxjnm <runtime-evidence-sha256>` and SHA256 field `chips.production_runtime_evidence_sha256`; both supplied by separately authorized operator, never environment default. Under Production advisory lock activate owner-only `chips_set_table_fence_active(true)` and switch future table eligibility default true atomically; retain existing false rows. Record only E2 history. Add valid/denied activation cases to existing migration test. Dependency: T005; application is blocked at T016 until owner/runtime evidence.

**US1 checkpoint**: E1 is independently reviewable and contract-tested; E2 cannot execute by merge/automatic Stage apply. No Production mutation yet. The current WS missing-column failure has a concrete schema fix without writer workaround.

## Phase 3 — shared pipeline and dark Production entry (PR A, US2)

- [ ] T007 [US2] Add `_shared/chips-ledger-retention-profile.mjs` under `scripts/ops/` with immutable Stage/Production identity/policy/SQL-name/lock/bounds maps and `resolveRetentionProfile`/`validateRetentionEnvironment`. Production accepts only `SUPABASE_PROD_DB_URL`, `SUPABASE_PROD_URL`, `SUPABASE_PROD_SERVICE_ROLE_KEY`, `DEPLOYED_COMMIT_SHA`, explicit Production switch; reject Stage/generic fallback and transaction pooler6543. Stage `validateStageEnvironment` remains rejecting Production. Dependency: T002 and fixed data-model.md. Existing coverage: `tests/chips/chips-ledger-stage-automation.test.mjs`, target mismatch and credential boundary only.
- [ ] T008 [US2] Extend `scripts/ops/chips-ledger-archive-export.mjs` functions `resolveTarget`, `maxBatchSizeForTarget`, `resolveOptions`, `readSnapshot`, `runExport`, `buildManifest` and policy literals in existing SQL selectors to use approved profile binding. Preserve current selection/age/USER/escrow/order/cursor and serialization; reject truncated whole-table canary, arbitrary cap/env override and Stage policy under Production. Manual Production max remains2; activated max5000 resolved only from fresh DB control through explicit profile context. Extend `tests/chips/chips-ledger-archive-export.test.mjs` for these exact target/cap/complete-table boundaries. Dependency: T007 and T004.
- [ ] T009 [US2] Extend `scripts/ops/chips-ledger-archive-store.mjs` (`resolveStorageTarget`, `verifyLocalArchive`, `loadOrCreatePendingBatch`, `storeArchive`) and `scripts/ops/chips-ledger-archive-prune.mjs` (`targetPolicy`, `parseManifestRow`, `assertTargetIdentity`, `buildPruneEvidence`, `createPruneStore`, `pruneArchive`, `executeArchivePrune`) to accept only profile-bound Production policy/equivalent SQL. Preserve private bucket/paths/bytes/SHA/MIME/no-overwrite, manual cap2 and Stage restrictions. Extend `tests/chips/chips-ledger-archive-storage.test.mjs` and `chips-ledger-archive-pruning.test.mjs`: cross-target rejection, no execute without both full durable copies, complete already_pruned vs partial receipt, cap enforcement at proof/execute. Dependency: T008.
- [ ] T010 [US2] Extract reusable Stage cycle/recovery implementation into `scripts/ops/_shared/chips-ledger-retention-cycle.mjs` with explicit profile arguments: `findOwnCycle`, `inspectDurableRecoveryState`, `persistDurableRecovery`, durable/resume assertions, `executeVerifiedCycle`, `resumeOwnCycle`, lock/identity/retry helpers and common bot/human automatic cycle functions listed in plan.md. Add `runRetentionCycle`; adapt existing `chips-ledger-stage-automation.mjs` wrappers/reexports without changing Stage modes, canary15/334 repairs, schedule or bounds. Production one-batch limit enforced across resume/new selection, no draining loop. Existing `chips-ledger-stage-automation.test.mjs` covers lock loss, OFF, ambiguity, no-candidate and known/unknown commit retry boundaries. Dependency: T009. Expected: all existing Stage fundamental contracts remain green.
- [ ] T011 [US2] Extract shared escrow internals from `scripts/ops/chips-ledger-stage-escrow-retention.mjs` into `scripts/ops/_shared/chips-ledger-escrow-retention.mjs`: classification, retry, candidate/snapshot/recovery verification, exact account revalidation, SQL retirement and bounded loop; retain original Stage exports/behavior. Production profile points to `chips_retire_production_escrow_accounts` and fresh Production canary row, same Production advisory lock, allowed archive policy bot-only, max1 batch/2 accounts. Extend existing `tests/chips/chips-ledger-stage-escrow-retention.test.mjs` for wrong target/nonzero/referenced account rejection and exact snapshot-before-delete/retry receipt. Dependency: T010 and E1 functions T004. Do not broaden to human escrow.
- [ ] T012 [US2] Add thin `scripts/ops/chips-ledger-production-automation.mjs` exporting `runProductionAutomation`; fixed `--policy existing-30d|bot-only-7d|closed-human-30d|escrow` and `--mode diagnostic|prepare|canary|automatic`. Exact canary arguments `--batch-id`, `--confirmation`, and escrow `--account-ids-sha256`/`--recovery-object-path` bind actual reviewed evidence; no default batch, Stage repair or arbitrary target. Diagnostic read-only, prepare no deletion, canary owner-authorized <=2, automatic double-gated. Reuse Production profile and cycle/escrow modules; klog aggregate summary with SHA/identity/receipt outcomes. Extend existing automation test for mode separation and wrong/absent authorization, no new glue suite. Dependency: T010–T011.
- [ ] T013 [US2] Add `.github/workflows/chips-ledger-production-scheduled-automation.yml`: dispatch-only, explicit policy/mode/exact canary inputs, protected `production-ledger` job environment, main/exact SHA, owner manual actor gate, Production-only secrets, `CHIPS_LEDGER_PRODUCTION_AUTOMATION_ENABLED` missing/0 denies auto, `CHIPS_LEDGER_PRODUCTION_DISPATCH_ACTOR` allowlisted auto principal, concurrency `chips-ledger-production-automation` with cancel=false. Extend existing `tests/chips/chips-ledger-stage-automation.workflow.guard.test.mjs` for Production credentials, no push/PR/native schedule trigger, one policy, gate and concurrency while retaining Stage checks. Dependency: T012. No workflow dispatch or secret installation is authorized by implementation alone.
- [ ] T014 [US2] Run relevant existing migration/archive/automation/bot/human/escrow fundamental suites plus `tests/chips/chips-ledger-table-metadata-fence.test.mjs` using current WS and Netlify `postTransaction` with Production-equivalent fixture and active fence. Validate full-table boundaries, empty escrow and replay semantics with existing `chips-ledger-bot-only-retention.test.mjs`/`chips-ledger-closed-human-retention.test.mjs` cases parameterized to profile where needed. Run syntax/npm test/repo guards from plan.md and whitespace check; record actual passes vs skips in feature quickstart/research. Submit PR A without merge, explicitly dark/off and user runtime verification pending. Dependency: T005–T013. No broad UI/authenticated smoke.

## Phase 4 — owner-gated PR A operations and fresh canaries (US2)

These are later operational tasks, not implicit implementation permissions. Separate authorization for each target mutation; no task is complete merely because PR A merged.

- [ ] T015 [US2] OWNER GATE — after review/owner merge, obtain explicit exact E1/SHA/hash/Production authorization, take fresh read-only baseline and apply only E1 through session psql under Production lock. Verify columns, final catalog/ACL/RLS and real history, OFF/fence false/cap2, preserved historical batch1, account balances/sequences/registry. Inspect `journalctl -u ws-server.service --no-pager -n 200` for disappearance of missing lifecycle-column error without assuming successful cleanup. Record aggregate receipt in `research.md`; full evidence private. Dependency: T014 and owner authorization.
- [ ] T016 [US2] OWNER GATE — compare live Production Netlify/WS revisions and writer-source digests against T014-tested sources, audit all TABLE writer call sites, obtain user runtime compatibility result and exact E2 confirmation/runtime-evidence SHA. Apply only E2; verify fence active, global/policy automation still OFF, historical bot eligibility false and new-table default true. Owner runs relevant buy-in/cash-out/deferred leave/settlement and replay scenario; bounded journald must not show new binding failures. Record evidence. Dependency: T015 plus compatibility PASS and separate authorization. If writer code differs, update planned affected files and exact-SHA Preview verification before any activation.
- [ ] T017 [US2] OWNER GATE — diagnostic then separately authorize fresh existing30d prepare via `runProductionAutomation`, at most2 transactions selected by unchanged `PRUNABLE_CANDIDATE_SQL`. Verify private primary archive + recovery archive + recovery manifest, exact ID/hash/cutoff and dry-run; obtain exact batch GO, execute once, verify invariant deltas/receipt/already_pruned. Reject old Production batch1 as canary. Record private full proof and aggregate summary in `research.md`. Dependency: T016; automation staysOFF/cap2.
- [ ] T018 [US2] OWNER GATE — independently prepare and execute fresh bot-only7d and closed-human30d canaries using same pipeline, each <=2 transactions with complete table/registry/lifecycle proof. Require actual age and fenced eligibility; if no eligible small table, report waiting for naturally aged data, do not backfill/split/backdate/increase cap. Preserve USER-history boundary and full terminal checks; verify registry/lifecycle receipt and replay. Record exact fresh IDs/hashes in `research.md`. Dependency: T016 and separate per-batch authorization; does not require T017 to create candidates but all must PASS before T020.
- [ ] T019 [US2] OWNER GATE — authorize escrow prepare and exact account-set GO against fresh cleaned bot canary from T018, using `chips_authorize_production_escrow_account_retirement_canary` and shared snapshot/retirement path. Require <=2 accounts, zero balance, no hot references, terminal/missing table rule as existing contract, exact immutable account snapshot already downloaded/verified. Execute once through Production lock, verify account absence/receipt and retained balances, reverify replay. Record snapshot hash/private recovery location. Dependency: bot canary T018 and separate authorization. Do not use Stage15 or closed-human account as substitute.

**US2 checkpoint**: PR A remains useful dark/off independently. PR B cannot proceed until T017/T018/T019 all PASS; no-candidate is not PASS, natural waiting can be weeks. This is an evidence dependency, not an architectural decision left to executor.

## Phase 5 — activation PR B (US3)

- [ ] T020 [US3] Author E3 `supabase/production-migrations/20260914092000_chips_ledger_production_retention_activation.sql` only after fresh canaries PASS. Embed exact new Production canary batch/table/account-set/archive/recovery hashes from T017–T019; validate under same Production lock, E1/E2/fence/runtime evidence and all complete receipts. Activate bot/human/escrow singleton policies and global control enabled=true/max_transactions=5000 atomically via owner-only routines. Preserve manual null-policy cap2; no placeholder15/334/1 and no activation by unchecked env. Update manifest/history provenance and existing `tests/chips/chips.migration.test.mjs`/export/prune suites for <=2 before, <=5000 activated, >5000 denied and wrong receipt rollback. Dependency: T017–T019 PASS, not just PR A merge.
- [ ] T021 [US3] Add `infra/vps/production-retention/arcade-chips-ledger-production-dispatch.sh`, `.service`, `.timer`. Dispatch fixed Production workflow/main, mode automatic, policy determined only by UTC slot (02:09 daily existing; hourly:09 bot except02; :24 human; :39 escrow), local flock and GitHub active-run check. Read dedicated Actions-write token from protected external operator configuration, no DB/Storage token. Require switch1; API uncertainty fails without repeated dispatch. Persistent=false; no native Production cron. No edits to live Stage dispatcher or auto-install from `.github/workflows/infra-vps.yml`. Extend existing workflow guard test for ownership/one-policy/no-backlog/default-off boundaries. Dependency: T013, T020.
- [ ] T022 [US3] Verify PR B with existing fundamental suites and plan.md repo checks, ensure no unrelated runtime/config files, document explicit cap/fence/history/USER-retention/scheduler impacts and exact activation SQL hash in `quickstart.md`. Submit PR B without merge; distinguish implementation checks from pending owner operations. Dependency: T020–T021. No Production mutations by PR creation.

## Phase 6 — owner-gated activation, observation and handoff (US3)

- [ ] T023 [US3] OWNER GATE — after owner review/merge, refresh canary/runtime/identity evidence and separately authorize/apply exact E3. Verify own history, Production policies enabled and globalcap5000; keep GitHub workflow switch0 while confirming SQL state. Retain cap2 for manual null-policy API. Record exact SHA/hash/receipts. Dependency: T022 and explicit authorization; changed runtime or stale proof blocks.
- [ ] T024 [US3] OWNER GATE — configure protected `production-ledger` credentials, dedicated `CHIPS_LEDGER_PRODUCTION_DISPATCH_ACTOR` and external dispatcher token; separately authorize installation of T021 units/script and switch `CHIPS_LEDGER_PRODUCTION_AUTOMATION_ENABLED=1`. Verify Production timer is sole owner, Stage timer unchanged, no native Production schedule and PostgreSQL session lock/global workflow concurrency shared across modes. Never publish secret values. Dependency: T023 plus explicit operational authorization.
- [ ] T025 [US3] Observe normal scheduled invocation of each of the four classes, capture Actions run/SHA/project/system/one-batch bounds and complete post-check; no-candidate confirms scheduling only, destructive proof comes from canary. Verify independent wallet/accounting state, expected hot-row/account deltas, durable recovery and no unexpected TABLE errors. Record kill-switch procedure and remaining capacity horizon in `research.md`/`quickstart.md`; do not claim bounded all-ledger growth (#889 broader classes remain out of scope). Dependency: T024. No agent broad authenticated smoke; owner performs relevant runtime scenario.
- [ ] T026 [US3] Final own review: compare implementation to spec FR001–012/SC001–005 and inventory, remove unused extraction/config/tests, confirm only explicitly planned files changed, no legacy Production API/history repair/cap bypass/Stage credential leakage; record check results, final runtime SHA/HEAD and owner runtime evidence. State pending verification honestly and do not merge. Dependency: T025 for rollout completion; implementation-only handoff may occur at T014/T022 with later owner-gated tasks unchecked.

## Dependency / parallel execution

Critical path: T001→T002→T003→T004→T005→T006, then T007→T008→T009→T010→T011→T012→T013→T014→T015→T016→T017/T018→T019→T020→T021→T022→T023→T024→T025→T026. T007 can start after T002 in parallel with schema work, but T008 depends on T004. T017 and T018 can be prepared as separate reviews; actual Production work is serialized by shared lock and independent authorizations. No parallel SQL edits or same-file extraction tasks. No [P] markers needed for otherwise overlapping code.

US1 provides a independently testable schema catch-up; US2 provides bounded canary tooling atop it; US3 requires their real evidence before activation. PR A includes T001–T014 code; T015–T019 are separately authorized operations. PR B includes T020–T022 code; T023–T025 are separately authorized operations. No extra implementation PR is presumed.

## Requirement coverage

| Requirement | Tasks |
|---|---|
| FR001 inventory/live evidence | T001,T002,T015 |
| FR002 history/equivalence | T002–T006,T020 |
| FR003 Stage isolation | T002,T007,T010,T013,T021 |
| FR004 identity gates | T004,T006–T009,T013,T015–T019 |
| FR005 runtime/fence | T003–T006,T014,T016 |
| FR006 reuse/four policies | T007–T012,T017–T019 |
| FR007 OFF/cap/canary | T004,T008,T012,T013,T017–T020,T023 |
| FR008 Storage/credentials | T009,T011,T013,T019,T024 |
| FR009 lock/resume/retry | T004,T010–T013,T021,T024 |
| FR010 accounting/replay | T005,T009,T011,T014,T017–T019,T025 |
| FR011 history boundary | T008,T014,T018,T022 |
| FR012 authorization | T015–T019,T023–T026 |

All added tests are fundamental critical boundaries in existing suites. Do not translate template TDD or setup suggestions into UI tests, new framework/dependencies, generic ignore changes or live database fixtures.
