# Example preview WS env file for ws-server-preview.service
# Keep secrets out of git and use real values on VPS only.
NODE_ENV=production
WS_HOST=127.0.0.1
WS_PORT=3100
SUPABASE_URL=https://<project>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
